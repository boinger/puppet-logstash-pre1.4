  tcp {
    format     => "json"
    charset    => "UTF-8"
    port       => <%= scope.lookupvar('logstash::config::tcp_port')%>
    type       => "eventlog"
    ssl_enable => true
    ssl_cacert => "/etc/ssl/certs/ca.crt"
    ssl_cert   => "/etc/ssl/certs/logstash.crt"
    ssl_key    => "/etc/ssl/certs/logstash.key"
  }

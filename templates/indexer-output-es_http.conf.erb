  elasticsearch_http {
    host => "<%= scope.lookupvar('logstash::config::elasticsearch_http_host') %>"
  }

  statsd {
    tags => ['sshd']
    increment => 'sshd.logstash_events'
  }

  # nagios_nsca {
  #   tags => ['ingester_sftp']
  #   host => nagios.icix.io
  #   send_nsca_config => /etc/nagios/send_nsca.cfg
  #   nagios_status => "0"
  # }


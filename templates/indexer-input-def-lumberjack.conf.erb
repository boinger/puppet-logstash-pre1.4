  lumberjack {
    charset         => "UTF-8"
    port            => <%= scope.lookupvar('logstash::config::lumberjack_port')%>
    ssl_certificate => "/etc/ssl/certs/logstash.crt"
    ssl_key         => "/etc/ssl/certs/logstash.key"
  }
